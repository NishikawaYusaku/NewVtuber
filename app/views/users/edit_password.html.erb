<!-- 
<div class="container">
  <div class="row">
    <div class="col-lg-6 border mx-auto">
      <div class="d-flex mb-5 mt-5">
        <%= form_with model: @user, url: user_password_path, local: true do |f| %>
          <%= render 'shared/error_message', object: f.object %>
          <div class="mb-3">
            <%= f.label :password_current, "現在のパスワード", class: "form-label" %>
            <%= f.text_field :password_current, class: "form-control" %>
          </div>
          <div class="mb-3 mt-5">
            <%= f.label :password, "新しいパスワード", class: "form-label" %>
            <%= f.text_field :password, class: "form-control" %>
          </div>
          <div class="mb-3">
            <%= f.label :password_confirmation, "新しいパスワード（確認）", class: "form-label" %>
            <%= f.text_field :password_confirmation, class: "form-control" %>
          </div>
          <div class="text-center">
            <%= f.submit "保存", class: "btn btn-primary si-form-width" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
-->